export struct TabModel {
    title: string,
    id: int,
}

export struct MetricModel {
    lower-val: float,
    upper-val: float,
    curr-val: float,
    unit: string,
}

export struct InfoRowModel {
    label: string,
    value: string,
}